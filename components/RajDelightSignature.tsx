'use client';

import Image from 'next/image';
import { useEffect, useMemo, useState } from 'react';
import { allMenuItems, menuCategories } from '@/lib/menu';
import styles from './RajDelightSignature.module.css';
import fixes from './RajDelightThemeFixes.module.css';

const ZOMATO = 'https://www.zomato.com/chandausi/raj-delight-restaurant-chandausi-locality/order';
const SWIGGY = 'https://www.swiggy.com/city/chandausi/raj-delight-restaurant-chandausi-rest1102497';
const PHONE = 'tel:+917983148985';
const MAPS = 'https://www.google.com/maps/search/?api=1&query=Raj%20Delight%2015%20Lathi%20Bazar%20Chandausi';
const INSTAGRAM = 'https://www.instagram.com/raj_delight/';

const IMG = {
  hero: 'https://images.unsplash.com/photo-1585937421612-70a008356fbe?auto=format&fit=crop&w=1800&q=88',
  interior: 'https://img3.restaurantguru.com/c8b1-Restaurant-Raj-Delight-interior.jpg',
  paneer: 'https://images.unsplash.com/photo-1601050690597-df0568f70950?auto=format&fit=crop&w=1400&q=88',
  pizza: 'https://images.unsplash.com/photo-1513104890138-7c749659a591?auto=format&fit=crop&w=1400&q=88',
  dosa: 'https://images.unsplash.com/photo-1668236543090-82eba5ee5976?auto=format&fit=crop&w=1400&q=88',
  thali: 'https://images.unsplash.com/photo-1546833999-b9f581a1996d?auto=format&fit=crop&w=1400&q=88',
  drink: 'https://images.unsplash.com/photo-1544145945-f90425340c7e?auto=format&fit=crop&w=1400&q=88',
};

const featured = [
  { name: 'Tandoori Paneer Tikka', category: 'Tandoori Station', image: IMG.paneer, tag: 'Signature' },
  { name: 'Veg Hyderabadi Biryani', category: 'Biryani', image: IMG.hero, tag: 'Classic' },
  { name: 'Paneer Tikka Pizza', category: 'Continental', image: IMG.pizza, tag: 'Modern' },
  { name: 'Masala Dosa', category: 'South Indian', image: IMG.dosa, tag: 'South Indian' },
];

const visualCategories = ['Tandoori Station','Indian Main Course','Biryani','Chinese Appetizers','Continental','Delicious Burger','Italian Pasta','Special Thali','South Indian','Dessert','Variety Of Ice Cream','Drinks'];
function Arrow() { return <span aria-hidden="true">↗</span>; }
function InstagramIcon({ size = 18 }: { size?: number }) { return <svg viewBox="0 0 24 24" width={size} height={size} aria-hidden="true"><rect x="3.5" y="3.5" width="17" height="17" rx="5" fill="none" stroke="currentColor" strokeWidth="1.7" /><circle cx="12" cy="12" r="4" fill="none" stroke="currentColor" strokeWidth="1.7" /><circle cx="17.2" cy="6.8" r="1" fill="currentColor" /></svg>; }
function SafeImage({ src, alt, priority = false, className = '' }: { src: string; alt: string; priority?: boolean; className?: string }) { const [failed, setFailed] = useState(false); if (failed) return <div className={`${styles.imageFallback} ${className}`} role="img" aria-label={`${alt} unavailable`}>Raj Delight</div>; return <Image fill src={src} alt={alt} priority={priority} sizes="100vw" className={className} onError={() => setFailed(true)} />; }

export function RajDelightSignature() {
  const [theme, setTheme] = useState<'light' | 'dark'>('light');
  const [mobileMenu, setMobileMenu] = useState(false);
  const [query, setQuery] = useState('');
  const [activeCategory, setActiveCategory] = useState('All');
  const [openCategory, setOpenCategory] = useState<string | null>(null);
  const [revealed, setRevealed] = useState<Record<string, boolean>>({});
  const [scrollProgress, setScrollProgress] = useState(0);
  const [lightbox, setLightbox] = useState<{ src: string; alt: string } | null>(null);

  useEffect(() => { const saved = window.localStorage.getItem('raj-delight-theme'); if (saved === 'light' || saved === 'dark') setTheme(saved); }, []);
  useEffect(() => { window.localStorage.setItem('raj-delight-theme', theme); document.documentElement.dataset.theme = theme; }, [theme]);
  useEffect(() => { const onScroll = () => { const max = document.documentElement.scrollHeight - window.innerHeight; setScrollProgress(max <= 0 ? 0 : Math.min(100, (window.scrollY / max) * 100)); }; window.addEventListener('scroll', onScroll, { passive: true }); onScroll(); return () => window.removeEventListener('scroll', onScroll); }, []);
  useEffect(() => { const elements = Array.from(document.querySelectorAll<HTMLElement>('[data-reveal]')); if (!('IntersectionObserver' in window)) { elements.forEach((el) => { const id = el.dataset.reveal; if (id) setRevealed((value) => ({ ...value, [id]: true })); }); return; } const observer = new IntersectionObserver((entries) => entries.forEach((entry) => { if (!entry.isIntersecting) return; const id = (entry.target as HTMLElement).dataset.reveal; if (id) setRevealed((value) => ({ ...value, [id]: true })); observer.unobserve(entry.target); }), { threshold: 0.14 }); elements.forEach((element) => observer.observe(element)); return () => observer.disconnect(); }, []);
  useEffect(() => { const onKey = (event: KeyboardEvent) => { if (event.key === 'Escape') setLightbox(null); }; window.addEventListener('keydown', onKey); document.body.style.overflow = lightbox ? 'hidden' : ''; return () => { window.removeEventListener('keydown', onKey); document.body.style.overflow = ''; }; }, [lightbox]);

  const filtered = useMemo(() => { const needle = query.trim().toLowerCase(); return allMenuItems.filter((item) => (activeCategory === 'All' || item.category === activeCategory) && (!needle || `${item.name} ${item.category}`.toLowerCase().includes(needle))); }, [activeCategory, query]);
  const jump = (id: string) => { document.getElementById(id)?.scrollIntoView({ behavior: 'smooth', block: 'start' }); setMobileMenu(false); };
  const chooseCategory = (category: string) => { setActiveCategory(category); setOpenCategory(category); setMobileMenu(false); requestAnimationFrame(() => document.getElementById('menu-items')?.scrollIntoView({ behavior: 'smooth', block: 'start' })); };
  const clearFilters = () => { setQuery(''); setActiveCategory('All'); setOpenCategory(null); };
  const handleHeroPointer = (event: React.PointerEvent<HTMLElement>) => { if (event.pointerType === 'touch') return; const rect = event.currentTarget.getBoundingClientRect(); event.currentTarget.style.setProperty('--mx', `${((event.clientX - rect.left) / rect.width - 0.5) * 14}px`); event.currentTarget.style.setProperty('--my', `${((event.clientY - rect.top) / rect.height - 0.5) * 10}px`); };
  const resetHeroPointer = (event: React.PointerEvent<HTMLElement>) => { event.currentTarget.style.setProperty('--mx', '0px'); event.currentTarget.style.setProperty('--my', '0px'); };

  return <main className={`${styles.site} ${theme === 'dark' ? styles.dark : ''} ${fixes.siteFix}`}>
    <div className={styles.scrollProgress} style={{ transform: `scaleX(${scrollProgress / 100})` }} aria-hidden="true" /><div className={styles.edgeFrame} aria-hidden="true" /><div className={styles.grain} aria-hidden="true" />
    <header className={styles.nav}><div className={`${styles.wrap} ${styles.navInner}`}><button className={styles.brand} onClick={() => jump('home')} aria-label="Raj Delight home"><img src="/raj-delight-mark.svg" alt="" className={styles.brandMark} /><span><b>Raj Delight</b><small>Chandausi</small></span></button><nav className={`${styles.links} ${mobileMenu ? styles.linksOpen : ''}`} aria-label="Primary navigation">{['about','menu','gallery','contact'].map((id) => <button key={id} onClick={() => jump(id)}>{id}</button>)}</nav><div className={styles.navSpacer} /><button className={styles.theme} onClick={() => setTheme((value) => value === 'light' ? 'dark' : 'light')} aria-label="Toggle visual theme"><span>{theme === 'light' ? 'Night' : 'Day'}</span></button><a href={ZOMATO} target="_blank" rel="noreferrer" className={styles.navOrder}>Order <Arrow /></a><button className={styles.burger} onClick={() => setMobileMenu((value) => !value)} aria-label="Toggle navigation" aria-expanded={mobileMenu}><span /><span /></button></div></header>
    <section className={styles.hero} id="home" onPointerMove={handleHeroPointer} onPointerLeave={resetHeroPointer}><div className={styles.heroMedia}><SafeImage src={IMG.hero} alt="Vegetarian biryani served at Raj Delight" priority className={styles.heroImage} /></div><div className={styles.heroOverlay} /><div className={styles.heroOrb} aria-hidden="true" /><div className={`${styles.wrap} ${styles.heroContent}`}><p className={styles.kicker}>15 Lathi Bazar · Chandausi · 100% Vegetarian</p><h1>A taste worth<br /><em>remembering.</em></h1><p className={styles.heroIntro}>North Indian favourites, South Indian classics, Indo-Chinese plates, pizzas, pasta, desserts and refreshing drinks.</p><div className={styles.heroActions}><button onClick={() => jump('menu')} className={styles.primaryCta}>Explore menu <Arrow /></button><button onClick={() => jump('gallery')} className={styles.secondaryCta}>View the place <Arrow /></button></div><div className={styles.heroMeta}><div><span>Rating</span><strong>4.3</strong></div><div><span>Categories</span><strong>{menuCategories.length}</strong></div><div><span>Hours shown</span><strong>9:30 AM–12 AM</strong></div></div></div><div className={styles.heroSide}>Scroll to discover <span /></div></section>
    <section className={styles.marquee} aria-label="Restaurant highlights"><div className={styles.marqueeTrack}><span>Indian classics</span><i>◆</i><span>South Indian</span><i>◆</i><span>Indo-Chinese</span><i>◆</i><span>Continental</span><i>◆</i><span>Desserts & drinks</span><i>◆</i><span>Indian classics</span><i>◆</i><span>South Indian</span><i>◆</i><span>Indo-Chinese</span><i>◆</i><span>Continental</span><i>◆</i><span>Desserts & drinks</span><i>◆</i></div></section>
    <section className={`${styles.story} ${styles.section} ${styles.reveal} ${revealed.story ? styles.show : ''}`} id="about" data-reveal="story"><div className={`${styles.wrap} ${styles.storyGrid}`}><button className={styles.storyImage} onClick={() => setLightbox({ src: IMG.interior, alt: 'Raj Delight restaurant interior' })} aria-label="View Raj Delight interior"><SafeImage src={IMG.interior} alt="Raj Delight restaurant interior" /><span>Public listing photo · view larger</span></button><div className={styles.storyCopy}><p className={styles.kicker}>The Raj Delight experience</p><h2>A little bit of <em>everything.</em></h2><p>Raj Delight brings together familiar Indian favourites with South Indian, Indo-Chinese, Continental and seasonal selections, all in one vegetarian menu.</p><div className={styles.storyFacts}><div><b>01</b><span>Vegetarian menu</span></div><div><b>02</b><span>Indoor seating</span></div><div><b>03</b><span>Takeaway & delivery</span></div><div><b>04</b><span>Lunch & dinner</span></div></div></div></div></section>
    <section className={`${styles.signature} ${styles.section} ${styles.reveal} ${revealed.signature ? styles.show : ''}`} data-reveal="signature"><div className={styles.wrap}><div className={styles.sectionHead}><div><p className={styles.kicker}>The table, reimagined</p><h2>Signature <em>flavours.</em></h2></div><button onClick={() => jump('menu')} className={styles.textButton}>Open complete menu <Arrow /></button></div><div className={styles.signatureGrid}>{featured.map((item, index) => <article className={styles.dish} key={item.name}><button className={styles.dishImage} onClick={() => setLightbox({ src: item.image, alt: item.name })} aria-label={`View ${item.name}`}><SafeImage src={item.image} alt={item.name} /><b>{String(index + 1).padStart(2, '0')}</b></button><p className={styles.dishTag}>{item.tag}</p><h3>{item.name}</h3><button onClick={() => chooseCategory(item.category)} className={styles.dishLink}>Explore category <Arrow /></button></article>)}</div></div></section>
    <section className={styles.menuSection} id="menu"><div className={styles.wrap}><div className={styles.menuIntro}><div><p className={styles.kicker}>The complete menu</p><h2>Every flavour.<br /><em>One place.</em></h2><p>Explore the full public menu through an editorial category system, then open any chapter for every listed dish.</p></div><div className={styles.menuCount}><span>Dishes</span><strong>{allMenuItems.length}</strong><small>across {menuCategories.length} categories</small></div></div><div className={styles.menuTools}><label className={styles.searchBox}><span>⌕</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search a dish or category" aria-label="Search a dish or category" />{query && <button onClick={() => setQuery('')} aria-label="Clear search">×</button>}</label><div className={styles.categoryScroller}><button className={activeCategory === 'All' ? styles.categoryActive : ''} onClick={clearFilters}>All</button>{visualCategories.map((category) => <button key={category} className={activeCategory === category ? styles.categoryActive : ''} onClick={() => chooseCategory(category)}>{category}</button>)}</div></div><div className={styles.menuChapters} aria-label="Featured menu categories">{visualCategories.map((category, index) => { const count = menuCategories.find((entry) => entry.name === category)?.items.length ?? 0; const image = category === 'Biryani' ? IMG.hero : category === 'South Indian' ? IMG.dosa : category === 'Continental' ? IMG.pizza : category === 'Special Thali' ? IMG.thali : category === 'Drinks' ? IMG.drink : IMG.paneer; return <button key={category} className={styles.chapter} onClick={() => chooseCategory(category)}><SafeImage src={image} alt={`${category} menu visual`} /><span className={styles.chapterShade} /><b>{String(index + 1).padStart(2, '0')}</b><span className={styles.chapterCopy}><small>{count} dishes</small><strong>{category}</strong><em>Explore <Arrow /></em></span></button>; })}</div><div className={styles.menuMeta}><span>{filtered.length} dishes shown</span>{activeCategory !== 'All' || query ? <button onClick={clearFilters}>Reset view</button> : null}</div><div id="menu-items">{menuCategories.map((category, index) => { const items = filtered.filter((item) => item.category === category.name); if (activeCategory !== 'All' && items.length === 0) return null; const open = openCategory === category.name || (!!query.trim() && items.length > 0); return <section key={category.name} className={styles.menuRow}><button className={styles.menuRowHead} onClick={() => setOpenCategory(open ? null : category.name)} aria-expanded={open}><span>{String(index + 1).padStart(2, '0')}</span><strong>{category.name}</strong><small>{category.items.length} items</small><b>{open ? '−' : '+'}</b></button><div className={`${styles.menuItems} ${open ? styles.menuItemsOpen : ''}`}>{open && items.map((item) => <a key={`${item.category}-${item.name}`} href={ZOMATO} target="_blank" rel="noreferrer"><span>{item.name}</span><small>Order <Arrow /></small></a>)}</div></section>; })}</div></div></section>
    <section className={`${styles.season} ${styles.section} ${styles.reveal} ${revealed.season ? styles.show : ''}`} data-reveal="season"><div className={`${styles.wrap} ${styles.seasonCard}`}><button className={styles.seasonImage} onClick={() => setLightbox({ src: IMG.thali, alt: 'Navratri thali visual' })} aria-label="View Navratri thali"><SafeImage src={IMG.thali} alt="Navratri thali visual" /><span>Seasonal selection</span></button><div className={styles.seasonCopy}><p className={styles.kicker}>Seasonal selection</p><h2>Navratri <em>Thali.</em></h2><p>The public menu includes a dedicated Navratri Food section with a listed Navratri Thali and seasonal favourites.</p><button className={styles.primaryCta} onClick={() => chooseCategory('Navratri Food')}>View Navratri selection <Arrow /></button></div></div></section>
    <section className={`${styles.gallery} ${styles.section} ${styles.reveal} ${revealed.gallery ? styles.show : ''}`} id="gallery" data-reveal="gallery"><div className={styles.wrap}><div className={styles.sectionHead}><div><p className={styles.kicker}>Visual journal</p><h2>See the place.<br /><em>Feel the mood.</em></h2></div><p className={styles.sectionAside}>A calm visual language: warm light, editorial crops, quiet captions and just enough motion to keep the experience alive.</p></div><div className={styles.galleryGrid}>{[IMG.interior, IMG.paneer, IMG.hero, IMG.pizza, IMG.dosa, IMG.thali].map((src, index) => <button key={src + index} className={index === 0 ? styles.galleryLarge : styles.galleryTile} onClick={() => setLightbox({ src, alt: index === 0 ? 'Raj Delight restaurant interior' : 'Raj Delight food visual' })} aria-label="Open gallery image"><SafeImage src={src} alt="Raj Delight gallery visual" /><span>{index === 0 ? 'Restaurant' : 'Food & mood'}</span></button>)}</div><a className={styles.galleryMore} href={MAPS} target="_blank" rel="noreferrer">View more public listing photos <Arrow /></a></div></section>
    <section className={styles.visit} id="contact"><div className={`${styles.wrap} ${styles.visitGrid}`}><div><p className={styles.kicker}>Visit · order · connect</p><h2>Come for the food.<br /><em>Stay for the moment.</em></h2><p>15, Lathi Bazar, Ward 05, Chandausi Locality, Chandausi, Uttar Pradesh 244412.</p><div className={styles.visitActions}><a href={MAPS} target="_blank" rel="noreferrer" className={styles.primaryCta}>Get directions <Arrow /></a><a href={PHONE} className={styles.secondaryCta}>Call restaurant</a></div></div><div className={styles.contactCard}><div><small>Hours shown online</small><strong>9:30 AM – 12:00 AM</strong></div><div><small>Phone</small><strong>+91 79831 48985</strong></div><div><small>Order online</small><strong>Zomato · Swiggy</strong></div><a href={INSTAGRAM} target="_blank" rel="noreferrer" className={styles.instagram}><InstagramIcon size={28} /><span>@raj_delight</span><b><Arrow /></b></a></div></div></section>
    <footer className={styles.footer}><div className={styles.wrap}><div className={styles.footerTop}><div className={styles.footerBrand}><img src="/raj-delight-mark.svg" alt="Raj Delight" /><div><b>Raj Delight</b><span>Chandausi</span></div></div><p>Vegetarian dining in Chandausi.<br />A little bit of everything.</p></div><div className={styles.footerColumns}><div><small>Explore</small>{['home','about','menu','gallery','contact'].map((id) => <button key={id} onClick={() => jump(id)}>{id}</button>)}</div><div><small>Order</small><a href={ZOMATO} target="_blank" rel="noreferrer">Zomato <Arrow /></a><a href={SWIGGY} target="_blank" rel="noreferrer">Swiggy <Arrow /></a><a href={PHONE}>Call <Arrow /></a></div><div><small>Social</small><a href={INSTAGRAM} target="_blank" rel="noreferrer"><InstagramIcon size={16} /> @raj_delight</a></div></div><div className={styles.footerBottom}><span>© {new Date().getFullYear()} Raj Delight</span><span>Public restaurant information · Chandausi</span></div></div></footer>
    <nav className={styles.mobileBar} aria-label="Quick actions"><button onClick={() => jump('menu')}>Menu</button><a href={ZOMATO} target="_blank" rel="noreferrer">Order</a><a href={PHONE}>Call</a><a href={MAPS} target="_blank" rel="noreferrer">Directions</a></nav>
    {lightbox && <div className={styles.lightbox} role="dialog" aria-modal="true" aria-label="Image viewer" onClick={() => setLightbox(null)}><button className={styles.lightboxClose} onClick={() => setLightbox(null)} aria-label="Close">×</button><div className={styles.lightboxInner} onClick={(event) => event.stopPropagation()}><SafeImage src={lightbox.src} alt={lightbox.alt} /><p>{lightbox.alt}</p></div></div>}
  </main>;
}
